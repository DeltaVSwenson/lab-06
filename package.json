{
  "name": "models",
  "version": "1.0.0",
  "description": "",
  "main": "players-schema.js",
  "scripts": {
    "start":"json-server --watch db.json --id=_id",
    "test": "jest --verbose",
    "test-watch": "jest --watchAll --verbose",
    "lint": "eslint --verbose --coverage '**/*.js'"
  },
  "jest": {
    "testEnvironment": "node"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "eslint": "^6.4.0",
    "json-server": "^0.15.1",
    "mongodb-memory-server": "^5.1.5",
    "mongoose": "^5.7.1",
    "supertest": "^4.0.2"
  },
  "devDependencies": {
    "jest": "^24.9.0"
  }
}
